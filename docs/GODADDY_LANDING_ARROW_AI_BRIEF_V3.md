# GoDaddy Landing (brandgoblinai.com) — Arrow AI Brief **V3** (sync with the new app: Studio + orange CTA)

### V2 landed — the page is now on-message. V3 is a small DELTA: add Goblin Studio (real image
### creation), update the now-outdated "logo direction → hand to a designer" line, and make the
### primary CTA a bold orange to match the app. Audited June 21, 2026.
> Paste the "ARROW AI PROMPT" block into Airo. This is incremental — keep everything V2 already
> shipped; only make the changes below.

---

## WHAT CHANGED IN THE APP SINCE V2 (why V3 exists)
- **Goblin Studio shipped:** users now generate **real images** in-app — logo concepts, social
  graphics, product art — automatically on-brand (from their kit's palette/voice). The landing still
  implies you only get *words* + a logo *direction*. Biggest gap.
- **The app's primary action button is now vibrant orange** (the "Conjure" button — data-backed
  highest-converting CTA color). The landing's CTAs are purple/teal — make the primary CTA orange to
  match + boost conversion.
- The "Logo Direction — ready to hand to a designer or image tool" line is now **outdated**: Nix
  generates the actual logo/image. Update it.

## POSITIONING GUARDRAIL (unchanged)
Studio is **depth inside the product**, NOT a new advertised suite. Weave "it makes your visuals too"
into the existing story as ONE integrated beat — do not add a product grid or a "Goblin Studio" page.
Don't introduce energy/credits/limits language; frame image creation as part of the magic.

---

## ARROW AI PROMPT — paste this into Airo

> Update my BrandGoblin AI landing page. Keep everything as it is now (the "Watch your idea become
> real" hero, idea sparks, pricing, the addictive-loop section, etc.) — make only these changes:

### 1. PRIMARY CTA → bold orange, everywhere it's the main action
> Change the primary call-to-action buttons to a **vibrant orange** (hex around **#FF6B35**, warm and
> bright) instead of the current purple/teal — make them the boldest, most eye-catching element on the
> page so a visitor's eye goes straight to them. Use this same orange consistently for the main action
> on the hero, mid-page, and final CTA so it's always recognizable as "the button." Make the hero's
> primary button read **"✦ Start Creating — Free"** (keep a small "7 days of everything, no card" line
> near it). All of these still link to https://app.brandgoblinai.com/signup. Keep the secondary
> button ("See Example Brands") in a quieter style so the orange one dominates.

### 2. ADD: Nix now creates your real visuals (Goblin Studio) — woven in, not a new product
> In the "What Nix conjures for you" section, update the **Logo Direction** item to reflect that Nix
> now generates the **actual logo and brand images**, not just a direction:
> - Rename it **"Logo & Brand Images"** with subtext **"Real logos, social graphics, and product art —
>   generated on-brand, instantly."**
> Then add ONE short beat (a line or small highlight, NOT a product grid) somewhere in the value area,
> e.g.:
> - Headline: **"Now Nix designs the visuals, too."**
> - Body: **"Beyond words — generate real, on-brand logos, social graphics, and product art inside the
>   app. Your palette, your vibe, conjured into actual images. No designer, no other tools, no leaving
>   BrandGoblin."**
> Keep it integrated and magical — one beat, not a suite of products.

### 3. FIX the outdated "hand to a designer" language
> Anywhere the page says a logo/asset is "ready to hand to a designer or image tool," reword it — Nix
> now makes the image itself. Change to something like **"Nix generates the actual image for you —
> ready to use."**

### 4. (Optional) Tie the addictive-loop section to creating images
> In the "Once you start, you won't want to stop" loop section, you can add a nod to image creation —
> e.g. the "One More" step can mention **"reroll the logo, try a new style, generate the socials"** —
> so the loop reflects the real Studio experience. Keep it short.

### GLOBAL
> Keep it mobile-first and fast. Keep the playful, cheeky Nix tone. Never display made-up statistics,
> user counts, or testimonials. Every primary CTA still goes to https://app.brandgoblinai.com/signup.

---

## POST-EDIT CHECKLIST
- [ ] Primary CTA is bold **orange (#FF6B35-ish)**, repeated consistently, dominant in the hero.
- [ ] Hero primary button reads **"✦ Start Creating — Free"** with a small 7-day/no-card line.
- [ ] "Logo Direction" → **"Logo & Brand Images"** (real images, generated on-brand).
- [ ] A short **"Now Nix designs the visuals, too"** beat added (integrated, not a product grid).
- [ ] No "hand to a designer/image tool" language remains.
- [ ] No energy/credits/limits language introduced; no fake stats; CTAs → app `/signup`.
- [ ] Hard-reload live to confirm Airo published.

*V3 delta to GODADDY_LANDING_ARROW_AI_BRIEF_V2.md (which is now live). Orange CTA per 2026 conversion
research + app brand consistency. Studio woven in as one beat, not a suite. June 21, 2026.*
